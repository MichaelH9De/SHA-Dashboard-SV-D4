import Card from '@/components/ui/Card'

export default function FrameworkReportSummary() {
  return (
    <Card>
      <h3 className="text-lg font-semibold text-slate-950">Framework summary</h3>
      <p className="mt-2 text-sm text-slate-600">Compare delivery performance across frameworks, identify which measure families are overperforming, and flag dead zones before the client does.</p>
    </Card>
  )
}
