import Textarea from '@/components/ui/Textarea'

export default function ActionNotesField() {
  return <Textarea placeholder="Notes, delivery context and anything a PM will otherwise forget at the worst possible moment." />
}
